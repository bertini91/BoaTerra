import React from "react";
import { BrowserRouter, Router, Switch } from "react-router-dom";

const App = () => {
  return (
    <BrowserRouter>
      <Router>
        <Switch>
            <Route></Route>
        </Switch>
      </Router>
    </BrowserRouter>
  );
};

export default App;
